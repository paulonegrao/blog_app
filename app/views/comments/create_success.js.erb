<%# j method escapes Javascript which means it will put a \ before every " %>
$("#comments").prepend("<%= j(render('/comments/comment', {comment: @comment})) %>");
<% @comment = Comment.new %>
$("#new_comment").replaceWith("<%= j render 'form' %>");
$(".alert").remove();
<% flash[:notice] = 'Comment created successfully' %>
$("#top-div").prepend("<%= j render '/layouts/flash_messages' %>");
<% flash[:notice] = nil %>
